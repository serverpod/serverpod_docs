# Global options

The global options of `scloud` can be specified anywhere on the command line,
in conjunction with any subcommand.

They allow you to override the default behavior, file locations,
and URLs that `scloud` uses.
