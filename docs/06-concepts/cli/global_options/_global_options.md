# Global options

The global options of `serverpod` can be specified anywhere on the command line, in conjunction with any command.

They control output verbosity, analytics, interactive prompts, and experimental features.
